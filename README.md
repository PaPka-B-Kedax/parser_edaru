
# 🍲 Culinary Aggregator API

Микросервисное приложение для агрегации, хранения и анализа кулинарных рецептов.
Проект включает в себя асинхронный парсер, REST API на FastAPI и базу данных PostgreSQL.

## 🛠 Технический стек

* **Language:** Python 3.13
* **API Framework:** FastAPI, Pydantic
* **Database:** PostgreSQL 15, SQLAlchemy (Asyncio), Alembic (Migrations)
* **Parsing:** HTTPX (Async), JMESPath
* **DevOps:** Docker, Docker Compose, GitHub Actions (CI/CD ready)

## 🚀 Функционал

1. **ETL Pipeline:** Автоматический сбор рецептов с внешних источников (eda.ru).
2. **Smart Filtering:** Поиск рецептов по ингредиентам, названию, времени готовки и категориям.
3. **Database:** Нормализованная схема данных (Рецепты <-> Ингредиенты <-> Категории).
4. **Swagger UI:** Интерактивная документация API.

## 📦 Запуск проекта

### Через Docker (Рекомендуется)

1. Клонируйте репозиторий:

   ```bash
   git clone [https://github.com/PaPka-B-Kedax/parser_edaru.git](https://github.com/PaPka-B-Kedax/parser_edaru.git)
   cd parser_edaru
   ```
2. Создайте файл `.env`:

   ```bash
   cp .env.example .env
   # Укажите свои данные внутри
   ```
3. Запустите контейнеры:

   ```bash
   docker compose up -d
   ```

API будет доступно по дресу: `http://localhost:8000/docs`

### Локальный запуск (для разработки)

1. Создайте виртуальное окружение: `python -m venv venv`
2. Активируйте: `source venv/bin/activate` (Linux/Mac) или `venv\Scripts\activate` (Win)
3. Установите зависимости: `pip install -r requirements.txt`
4. Примените миграции: `alembic upgrade head`
5. Запустите сервер: `uvicorn main:app --reload`

## 👨‍💻 Автор

PaPka_B_Kedax — Backend Developer
